/**
 * STORY-132 P5/T4: Format label under ad preview — discrete UI caption (e.g. "Viber / IG Story · 1080 × 1920").
 * No black background; muted text so it reads as UI metadata, not part of the ad.
 * Used only by AgentChat preview pane. Internal — do not import from other app code.
 */
export const PREVIEW_FORMAT_LABEL_CLASS = 'text-xs text-gray-500 text-center select-none';
